extends layout

block content
    if level
        h1 #{level.levelNumber}
        p Here are your full details:
        pre= JSON.stringify(level, null, 2)
    else
        h1 There's no such level!
    script.
        console.log(!{JSON.stringify(level)})